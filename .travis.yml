os:
  - linux
  - osx
language: generic
sudo: required
dist: trusty
osx_image: xcode8
addons: mysql
script:
  - eval "$(curl -sL https://swift.vapor.sh/ci)"
  - eval "$(curl -sL https://swift.vapor.sh/codecov)"
